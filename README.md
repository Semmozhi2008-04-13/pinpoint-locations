# Saved Locations — Frontend Engineer Assignment

A React + TypeScript web app for saving favorite places on an interactive map.

## Setup

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production build into dist/